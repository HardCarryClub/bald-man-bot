{
  "name": "bald-man-bot",
  "private": true,
  "scripts": {
    "dev": "bun run build && bun run start:dev",
    "register": "bun run build -r && bun run start:dev",
    "build": "bunx dressed build -i",
    "start:dev": "node .dressed/index.mjs"
  },
  "dependencies": {
    "discord-api-types": "^0.38.16",
    "discord-fmt": "^1.0.0",
    "dressed": "1.9.0-rc.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.1.2",
    "@types/bun": "latest",
    "@types/node": "^24.0.15",
    "lefthook": "^1.12.2",
    "tsx": "^4.20.3"
  },
  "trustedDependencies": ["esbuild", "lefthook"]
}
